machine:
  python:
    version: 3.6.0

  dependencies:
    pre:
      - pip install pipenv
      - pipenv install
      - sleep 5

    test:
      override:
        - pipenv run py.test test.py
        - exit 0
