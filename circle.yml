machine:
  python:
    version: 3.7.0

  dependencies:
    pre:
      - pip install pipenv
      - pipenv install
      - pipenv run python main.py runserver:
          background: true
      - sleep 5

    test:
      override:
        - pipenv run py.test test.py
        - exit 0
